method sample_func(){
    anew num a = 3;
    anew dec b = 3.5;
    anew bool c = indeed;
    anew num d = a * 4;

    perhaps( a > d || c ){
        proclaim("ABC");
    }however(d >= a && b < 0 ){
        proclaim("DEF");
    }otherwise{
        proclaim("GHI");
    }

    anew str e = in();

    yield 3;
}