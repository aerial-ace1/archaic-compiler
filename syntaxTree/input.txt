# This is a comment
num sample () {
    anew num a = 3;
    anew dec b = 34.6364;
    anew bool c = indeed;
    anew char f ='h';
    anew num d = a * b;
    perhaps( a > d && c == b || x < inquire() ){
        proclaim("ABC");
    }otherwise{
        d = inquire();
        proclaim(e);
    }

    whilst( a > 5){
        a = a + 1;  
    }


    yield 3;
}